{
  "author": "Philip Hofstetter",
  "name": "resque-runner",
  "description": "A general purpose worker for resque. Shelling out to an arbitrary script and using blpop instead of polling",
  "version": "0.0.1",
  "engines": {
    "node": ">=0.6.0"
  },
  "dependencies": {
    "redis": ">=0.7.0",
    "coffee-script": ">=1.2.0"
  },
  "bin": {
    "resque-runner": "./bin/resque-runner.js"
  },
  "optionalDependencies": {
    "hiredis": ">=0.1.13"
  }
}
